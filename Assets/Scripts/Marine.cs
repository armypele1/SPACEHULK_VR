using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[System.Serializable]
public class Marine : GamePiece
{
    // Start is called before the first frame update
    void Awake()
    {
        actionPoints = 4;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
